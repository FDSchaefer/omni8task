version: '3.8'

services:
  skull-stripper:
    build: .
    volumes:
      - ./data/input:/data/input
      - ./data/output:/data/output
      - ./docker_config.json:/data/config/config.json:ro
    environment:
      - PYTHONUNBUFFERED=1
    command: '--watch'
    restart: unless-stopped